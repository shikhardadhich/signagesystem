node_modules/
uploads/*
!uploads/.gitkeep
.firebase/
firebase-debug.log
*-debug.log
.vercel

# Never commit credentials — Supabase keys and the like belong in the host's
# environment variables, not in the repo.
.env
.env.*
!.env.example

# Local cafe + board data written by the app (local driver only).
data/

# Board images uploaded through the admin, not part of the repo.
public/assets/board/
